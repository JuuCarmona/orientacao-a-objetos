from quadrado import Quadrado
from circulo import Circulo

forma1  = Quadrado(cor = "vermelho",base = 10, altura = 2)
forma2 = Circulo(cor = "verde",raio = 5)

forma1.calcular_area()
forma2.calcular_area()
